{
  "name": "to-do-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd clientt && npm install)",
    "server": "node ./server/server.js",
    "client":"cd clientt && npm start"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
